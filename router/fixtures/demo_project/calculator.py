"""Small arithmetic helpers used by the isolated router fixture."""


def increment(value: int) -> int:
    """Return ``value`` plus one."""
    return value + 1


def double(value: int) -> int:
    """Return ``value`` multiplied by two."""
    return value * 2
